// Import MMKV
import type {MMKVConfiguration} from 'react-native-mmkv';

const storageConfig: MMKVConfiguration = {
  id: 'app-storage',
  encryptionKey: 'simprarn-root-key',
};

export default storageConfig;
